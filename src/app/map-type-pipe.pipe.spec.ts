import { MapTypePipePipe } from './map-type-pipe.pipe';

describe('MapTypePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MapTypePipePipe();
    expect(pipe).toBeTruthy();
  });
});
